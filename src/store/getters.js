export default {

  // 统计数量
  // totleCount(state) {
  //   let count = 0;
  //   state.goodsList.forEach(goods => {
  //     count += goods.count;
  //   })
  //   return count;
  // },

  // 统计价格
  // totlePrice(state) {
  //   let price = 0;
  //   state.goodsList.forEach(goods => {
  //     price += parseFloat(goods.price) * goods.count;
  //   })
  //   return price;
  // }
}